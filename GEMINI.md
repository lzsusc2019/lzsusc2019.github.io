# LZS Blog 开发与维护规约 (v3.0)

## 🎯 核心哲学 (Philosophy)
**第一要义：博客是服务于“我”的。**
- 所有的内容必须以“快速回想”为终极目标。
- 拒绝废话，直击痛点。
- 看到标题和第一段，必须能立即唤起当时的解题思路或操作场景。

## ✍️ 博文排版风格规范 (Writing Style)

### 1. 结构化“回想”模块
- **Quick Recall 模块**：每篇文章开头必须使用 `{: .prompt-info }` 或 `{: .prompt-tip }` 总结核心公式、口诀或操作关键点。
- **复杂度摘要**：算法类文章需在标题下或代码块前标注 `时间复杂度 / 空间复杂度`。

### 2. 标题与层次 (Hierarchy)
- **H1 禁止**：正文严禁使用 `#`。
- **H2 起步**：所有顶级章节必须使用 `##`。
- **呼吸感**：标题前后必须保留空行，长段落需进行拆分。

### 3. 代码块高级化 (Code Style)
- **显式高亮**：严禁裸代码块，必须标注语言（`java`, `bash`, `sql` 等）。
- **文件名路径**：复杂代码块首行建议使用 `// filepath: ...` 或 `// 场景：...` 标注。
- **逻辑注释**：代码关键行需添加简短、精准的中文注释，解释“为什么这么写”。

### 4. 视觉增强 (Visuals)
- **重点术语**：在文中首次出现的关键术语需使用 **加粗** 或 `行内代码`。
- **Chirpy Callouts**：
  - 提示：`{: .prompt-tip }`
  - 警告/避坑：`{: .prompt-warning }`
  - 关键点：`{: .prompt-info }`

## 🛠 技术维护准则 (Maintenance)
1. **数据原子性**：禁止在 Shell 环境下运行不可预测的批量正文修改脚本。
2. **字符洁净度**：首行 `---` 前后严禁存在任何不可见字符（如 `\x01`）。
3. **路径稳定性**：所有博文必须具备永久链接 `permalink: /posts/YYYYMMDD-title/`。
4. **侧边栏级联**：分类必须采用 `[父分类, 子分类]` 格式。
