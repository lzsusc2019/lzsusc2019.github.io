# LZS Blog 开发与维护规约 (v3.0)

## 🎯 核心哲学 (Philosophy)
**第一要义：博客是服务于“我”的。**
- 所有的内容必须以“快速回想”为终极目标。
- 拒绝废话，直击痛点。
- 看到标题和第一段，必须能立即唤起当时的解题思路或操作场景。

## ✍️ 博文排版风格规范 (Writing Style)

### 1. 结构化“回想”模块
- **Quick Recall 模块**：每篇文章开头必须使用 `{: .prompt-info }` 或 `{: .prompt-tip }` 总结核心公式、口诀或操作关键点。
- **复杂度摘要**：算法类文章需在标题下或代码块前标注 `时间复杂度 / 空间复杂度`。

### 2. 标题与层次 (Hierarchy)
- **H1 禁止**：正文严禁使用 `#`。
- **H2 起步**：所有顶级章节必须使用 `##`。
- **呼吸感**：标题前后必须保留空行，长段落需进行拆分。

### 3. 代码块高级化 (Code Style)
- **显式高亮**：严禁裸代码块，必须标注语言（`java`, `bash`, `sql` 等）。
- **文件名路径**：复杂代码块首行建议使用 `// filepath: ...` 或 `// 场景：...` 标注。
- **逻辑注释**：代码关键行需添加简短、精准的中文注释，解释“为什么这么写”。

### 4. 视觉增强 (Visuals)
- **重点术语**：在文中首次出现的关键术语需使用 **加粗** 或 `行内代码`。
- **Chirpy Callouts**：
  - 提示：`{: .prompt-tip }`
  - 警告/避坑：`{: .prompt-warning }`
  - 关键点：`{: .prompt-info }`

## 🛠 技术维护准则 (Maintenance)
1. **数据原子性**：禁止在 Shell 环境下运行不可预测的批量正文修改脚本。
2. **字符洁净度**：首行 `---` 前后严禁存在任何不可见字符（如 `\x01`）。
3. **路径稳定性**：所有博文必须具备永久链接 `permalink: /posts/YYYYMMDD-title/`。
4. **侧边栏级联**：分类必须采用 `[父分类, 子分类]` 格式。

## 🚨 血泪教训 (Post-Mortem: Do Not Repeat)
1. **严禁过度精简**：**绝对不可**在重构时主观删减用户的“题意”、“解析”、“参考代码”或多解法，用户的推导过程比所谓的“极简排版”更重要。
2. **严禁破坏索引**：Markdown 中的题目名称必须使用规范的 Headings（如 `### 题目名`），**绝不可**将其降级为普通文本，否则会导致右侧大纲（TOC）和分类索引完全丢失。
3. **拒绝贪快脚本**：在没有用户提供的“标准示例（Template）”前，**严禁**使用 Python/Sed 正则脚本去强行大批量格式化 1000 行以上的长文。长文重构必须先锁定 Template，再手动或极小颗粒度地稳健搬运。

## 📝 算法题标准模板 (Algorithm Post Template)
所有算法题重构或新增，**必须且只能**采用以下层级和格式，保留原有全部正文，只做标题符号替换，每道题间加 `---` 隔开：

```markdown
### [LeetCode {题号} {题目名称}]({真实的LeetCode链接})

#### 1、题意
这里是题目描述和示例...（保留用户的原汁原味）

#### 2、题目解析
这里是解题思路...（保留用户的推导过程）

#### 3、参考代码
\`\`\`java
// 必须加高亮标记，代码一字不删
\`\`\`

---
```

## 📊 深度研报写作规范 (横纵分析法)

当要求对技术、产品或商业模式进行深度研究和博文输出时，**必须严格采用以下「横纵分析法」**：

### 一、纵向分析（Diachronic / Longitudinal）
沿时间轴，完整还原发展全貌。要求如下：
1. **起源追溯**：它诞生的背景是什么？基于什么技术/理念/需求而来？
2. **诞生节点**：明确的首次发布/成立时间，最初的形态和定位。
3. **演进历程**：按时间顺序梳理关键节点（重大版本、战略转型、技术架构变化）。
4. **决策逻辑**：在每个关键节点上，尽可能还原决策背后的原因及当时的约束条件。
5. **叙事要求**：**不要写成干巴巴的年表**。用故事的方式把发展史串起来，让读者感受到因果关系。

### 二、横向分析（Synchronic / Cross-sectional）
以当前时间点为切面，与同赛道的竞品进行全面对比。
1. **核心差异对比**：技术路线、产品形态、目标用户、核心优势与短板。
2. **用户视角**：每个竞品的真实用户口碑如何？实际的使用方式和官方定位有没有偏差？
3. **生态位分析**：占据了什么位置？填补了什么空白？
4. **趋势判断**：在竞争格局中的走向是什么？机会和风险在哪里？

### 三、写作风格要求（极其重要）
1. **可读性优先**：写得像一篇优质的深度报道或非虚构特稿，有节奏感，有画面感。**坚决杜绝使用“- 优势：xxx”这种机械的列表格式**。
2. **叙事驱动，不是罗列驱动**：纵向部分要有故事弧线，有起承转合。不要写成流水账。
3. **用人话写**：避免咨询公司式的套话和空洞的形容词（如"赋能""抓手""颠覆"）。用具体的细节和例子代替概括性陈述。
4. **对比要有温度**：横向对比要讲清楚每个竞品"活成了什么样"，用户选它的真实理由是什么。
5. **去 AI 味道（极简克制）**：绝对禁止使用过度戏剧化的比喻（如"魔法咒语"、"驯兽师"、"通满高压电的钢铁围栏"）和情绪化的词汇（如"惊恐地发现"、"血泪史"、"狂热"）。保持客观、克制、务实的工程师视角。就事论事，少用感叹号，拒绝强行煽情。

### 四、输出格式要求
在报告最后，加一段**「横纵交汇」**的总结：把纵向发展脉络和横向竞争格局结合起来，给出当前所处位置和未来走向的判断。
